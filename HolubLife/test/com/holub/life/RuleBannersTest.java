package com.holub.life;

import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class RuleBannersTest
{
    @Test
    void init()
    {
        // given

        // when

        // then
        assertEquals(1, 1);
        //Assertions.assertThat(1).isEqualTo(1);
    }
}